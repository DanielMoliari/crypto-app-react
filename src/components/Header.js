import React from 'react'
import Helmet from 'react-helmet'

const Header = () => {
    return (
        <Helmet>
            <title>Crypto App</title>
        </Helmet>
    )
}

export default Header
